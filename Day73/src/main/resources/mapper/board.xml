<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itbank.model.BoardDAO">

	<select id="selectAll" 
			resultType="com.itbank.model.dto.BoardDTO"
			parameterType="com.itbank.service.Paging">
		select * from board		
			where ${type} like '%' || #{search} || '%'		
			order by ${order} desc   
			offset #{offset} rows 
			fetch first #{perBoard} rows only
	</select>
	
	<select id="selectOne"
			resultType="com.itbank.model.dto.BoardDTO"
			parameterType="int">
		select * from board where idx = #{idx}
	</select>
	
	<insert id="insert" parameterType="com.itbank.model.dto.BoardDTO">
		insert into 
			board(title, content, writer)
			values(#{title}, #{content}, #{writer})
	</insert>
	
	<delete id="delete" parameterType="int">
		delete from board where idx = #{idx}
	</delete>
	
	<update id="update" parameterType="com.itbank.model.dto.BoardDTO">
		update board
			set
				title = #{title}, content = #{content}, writer = #{writer} 
				where idx = #{idx}
	</update>
	
	<update id="countUp" parameterType="int">
		update board
			set
				view_count = view_count + 1
				where idx = #{idx}
	</update>
	
	<select id="getTotalBoard" 
			resultType="int"
			parameterType="com.itbank.service.Paging">	
 	 select count(*) from board
  		where ${type} like '%' || #{search} || '%'	
	</select>
</mapper>